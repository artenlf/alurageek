import styled from "styled-components";

const Link = styled.a`
  align-items: center;
  color: #2a7ae4;
  display: flex;
  font-size: 16px;
  font-weight: 700;
  transition: 0.3s;
  :hover {
    transform: scale(110%);
  }
`;

export default Link;
